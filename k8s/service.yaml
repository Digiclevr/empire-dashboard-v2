apiVersion: v1
kind: Service
metadata:
  name: empire-dashboard-svc
  namespace: nexia-empire
  labels:
    app: empire-dashboard
    component: nexia-supervisor
spec:
  type: ClusterIP
  ports:
  - port: 5002
    targetPort: 5002
    protocol: TCP
    name: http
  selector:
    app: empire-dashboard