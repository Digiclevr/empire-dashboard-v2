apiVersion: v1
kind: Service
metadata:
  name: blueocean-dashboard-svc
  namespace: nexia
  labels:
    app: blueocean-dashboard
    component: nexia-supervisor
spec:
  type: ClusterIP
  ports:
  - port: 5002
    targetPort: 5002
    protocol: TCP
    name: http
  selector:
    app: blueocean-dashboard